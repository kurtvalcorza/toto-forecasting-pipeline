# Release status

Current status: **Candidate / source-complete, awaiting clean-runtime execution**. The pipeline package (fleet snapshot scheme: `weights/toto-2.0-2.5b/dimer-base-manifest.json`, `verify_snapshot`, `stage_missing_files`), offline unit tests, `MODEL_CARD.md`, and the standalone `TASK-INFERENCE` tutorial `tutorials/toto_forecasting_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 1.1 §3.6) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The 9.8 GB checkpoint is not kept locally, so `verify_snapshot` over the real file is unexercised until the clean GPU run. The tutorial remains non-release-grade until clean-runtime execution evidence for the exact candidate commit is recorded according to `docs/release-verification.md`.
